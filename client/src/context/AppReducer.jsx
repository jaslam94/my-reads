// eslint-disable-next-line import/no-anonymous-default-export
export default (state, action) => {
  switch (action.type) {
    // case "WantToRead":
    // case "Read":
    default:
      return state;
  }
};
